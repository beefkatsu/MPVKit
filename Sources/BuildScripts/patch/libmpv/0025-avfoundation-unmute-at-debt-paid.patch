diff --git a/audio/out/ao_avfoundation.m b/audio/out/ao_avfoundation.m
index 87c7317..5eddab5 100644
--- a/audio/out/ao_avfoundation.m
+++ b/audio/out/ao_avfoundation.m
@@ -255,6 +255,12 @@ static void feed(struct ao *ao)
     int64_t cur_time_mp = mp_time_ns();
     int64_t end_time_av = MPMAX(p->end_time_av, cur_time_av);
 
+    if (p->renderer.muted && p->resume_settle_tb &&
+        cur_time_av >= p->resume_settle_tb) {
+        p->renderer.muted = NO;
+        MP_WARN(ao, "[clockdiag] unmute at debt-paid tb=%.3f\n", cur_time_av / 1e9);
+    }
+
     if (p->rate_engaged && !p->engage_verified) {
         if (cur_time_av - p->engage_tb > 50000000) {
             p->engage_verified = true; // timebase is really running
@@ -278,7 +284,6 @@ static void feed(struct ao *ao)
             cur_time_mp > p->resume_settle_until) {
             p->resume_settle_until = 0;
             p->resume_settle_refresh = true; // one cap-bypass read anchors truth
-            p->renderer.muted = NO;
             MP_WARN(ao, "[clockdiag] settle-done tb=%.3f (armed at %.3f)\n",
                     cur_time_av / 1e9, p->resume_settle_tb / 1e9);
         } else {
